# Scheduler (Round-Robin)

- **Algorithm**: simple round-robin.
- **Switch**: on `sched_yield()` call.
- **Selection**: next ready (Ready) task after the current one.

Future: timer interrupt for preemptive multitasking.
