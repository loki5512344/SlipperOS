# Переключение контекста

Сохраняем/восстанавливаем callee-saved регистры:
ra, sp, s0-s11.

## save_context

Записывает регистры в структуру Context.

## restore_context

Загружает регистры из Context и переходит по ra.

Переключение происходит только в sched_yield.
