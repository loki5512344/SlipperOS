# Bump Allocator

Самый простой аллокатор: указатель двигается вперёд, освобождения нет.

- Размер кучи: 1MB
- Используется как `#[global_allocator]`.
- Подходит для начального этапа, пока не заработает page allocator.

Когда переходим на page allocator — bump остаётся для мелких аллокаций ядра.
